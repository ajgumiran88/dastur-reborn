---
import Emblem from '@/components/ui/Emblem.astro';
import { nav } from '@/config/nav';
import { site } from '@/config/site';
import { t, type Locale } from '@/lib/i18n';
import { telLink, mailto, orderHref, isLivePhone } from '@/lib/links';
import { displayAreas, displayOrUnconfirmed } from '@/lib/placeholders';
import { preventWidows } from '@/lib/typography';

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
const year = new Date().getFullYear();
const platforms = site.deliveryPlatforms.filter((p) => p.enabled);
const socials = site.socials.filter((s) => s.enabled);
---

<footer id="contact" class="footer section--indigo">
  <div class="container footer__grid">
    <div class="footer__brand">
      <Emblem variant="lockup" tone="sand" locale={locale} class="footer__logo" />
      <p class="footer__statement measure--narrow">{preventWidows(t(locale, 'footer.statement'))}</p>
      <p class="footer__taglineText">{t(locale, 'footer.tagline')}</p>
    </div>

    <div class="footer__col">
      <h2 class="footer__h caps">{t(locale, 'footer.contact')}</h2>
      <ul role="list" class="footer__list">
        <li>
          {isLivePhone(site.contact.whatsapp) ? (
            <a href={orderHref(locale)} target="_blank" rel="noopener noreferrer">
              WhatsApp
            </a>
          ) : (
            <>
              WhatsApp
              <span class="placeholder-badge">{t(locale, 'placeholders.badge')}</span>
            </>
          )}
        </li>
        <li>
          {isLivePhone(site.contact.phone) ? (
            <a href={telLink(site.contact.phone)}>{site.contact.phone}</a>
          ) : (
            <>
              {displayOrUnconfirmed(locale, site.contact.phone)}
              <span class="placeholder-badge">{t(locale, 'placeholders.badge')}</span>
            </>
          )}
        </li>
        <li>
          {site.contact.email && !site.contact.email.includes('example') && site.contact.email.includes('@') ? (
            <a href={mailto(site.contact.email)}>{site.contact.email}</a>
          ) : (
            <>
              {displayOrUnconfirmed(locale, site.contact.email)}
              <span class="placeholder-badge">{t(locale, 'placeholders.badge')}</span>
            </>
          )}
        </li>
      </ul>
      <dl class="footer__meta">
        <div>
          <dt>{t(locale, 'delivery.fields.locations')}</dt>
          <dd>
            {displayAreas(locale, site.serviceAreas)}
            <span class="placeholder-badge">{t(locale, 'placeholders.badge')}</span>
          </dd>
        </div>
        <div>
          <dt>{t(locale, 'delivery.fields.hours')}</dt>
          <dd>
            {displayOrUnconfirmed(locale, site.hours)}
            <span class="placeholder-badge">{t(locale, 'placeholders.badge')}</span>
          </dd>
        </div>
      </dl>
    </div>

    <div class="footer__col">
      <h2 class="footer__h caps">{t(locale, 'footer.explore')}</h2>
      <ul role="list" class="footer__list">
        {nav.map((item) => (
          <li><a href={`#${item.id}`}>{t(locale, item.key)}</a></li>
        ))}
      </ul>
    </div>

    <div class="footer__col">
      {platforms.length > 0 && (
        <>
          <h2 class="footer__h caps">{t(locale, 'footer.order')}</h2>
          <ul role="list" class="footer__list">
            {platforms.map((p) => (
              <li>
                <a href={p.url} target="_blank" rel="noopener noreferrer" data-demo={p.demo ? '' : undefined}>
                  {p.name}
                </a>
              </li>
            ))}
          </ul>
        </>
      )}
    </div>

    <div class="footer__col">
      {socials.length > 0 && (
        <>
          <h2 class="footer__h caps">{t(locale, 'footer.follow')}</h2>
          <ul role="list" class="footer__list footer__social">
            {socials.map((s) => (
              <li>
                <a
                  href={s.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  aria-label={s.label}
                  title={s.label}
                >
                  {s.name === 'instagram' ? (
                    <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="1.8">
                      <rect x="3" y="3" width="18" height="18" rx="5" />
                      <circle cx="12" cy="12" r="4.2" />
                      <circle cx="17.5" cy="6.7" r="1" fill="currentColor" stroke="none" />
                    </svg>
                  ) : (
                    <svg viewBox="0 0 24 24" aria-hidden="true" fill="currentColor">
                      <path d="M14.2 3h3.1c.3 2.1 1.5 3.6 3.7 4.2v3.1a8 8 0 0 1-3.7-1.2v6.2A5.7 5.7 0 1 1 12.4 9v3.1a2.7 2.7 0 1 0 1.8 2.6V3Z" />
                    </svg>
                  )}
                  <span class="sr-only">{s.label}</span>
                </a>
              </li>
            ))}
          </ul>
        </>
      )}
    </div>
  </div>

  <div class="container">
    <p class="footer__allergen">{t(locale, 'footer.allergenNote')}</p>
  </div>

  <div class="container footer__bottom">
    <p class="footer__copy">© {year} {site.brand.legalName}. {t(locale, 'footer.rights')}</p>
    <ul role="list" class="footer__legal">
      <li><a href={site.legal.privacy}>{t(locale, 'footer.legal.privacy')}</a></li>
      <li><a href={site.legal.terms}>{t(locale, 'footer.legal.terms')}</a></li>
      <li><a href={site.legal.allergen}>{t(locale, 'footer.legal.allergen')}</a></li>
    </ul>
  </div>
</footer>

<style>
  .footer {
    padding-block: clamp(3.5rem, 6vw, 6rem) 2rem;
    position: relative;
    isolation: isolate;
  }
  .footer > * {
    position: relative;
    z-index: 1;
  }
  .footer__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem 2rem;
  }
  .footer__brand {
    display: flex;
    flex-direction: column;
    gap: 1.3rem;
    align-items: center;
    text-align: center;
    grid-column: 1 / -1;
  }
  .footer :global(.footer__logo) {
    width: clamp(100px, 24vw, 128px);
    max-width: 128px;
    align-items: center;
  }
  .footer__taglineText {
    margin-top: 0.35rem;
  }
  [dir='rtl'] .footer :global(.footer__logo) {
    align-items: center;
  }
  .footer__statement {
    color: var(--on-dark-muted);
    font-size: var(--fs-small);
    line-height: 1.7;
  }
  .footer__h {
    color: var(--brass-300);
    margin-bottom: 1rem;
    font-size: 0.72rem;
    line-height: 1.2;
    white-space: nowrap;
  }
  .footer__list {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
    list-style: none;
    margin: 0;
    padding: 0;
    font-size: var(--fs-small);
  }
  .footer__list a {
    color: var(--sand);
    opacity: 0.82;
    transition: opacity var(--dur-1) var(--ease-out);
  }
  .footer__list a:hover {
    opacity: 1;
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .footer__social {
    flex-direction: row;
    gap: 0.65rem;
  }
  .footer__social a {
    display: grid;
    place-items: center;
    width: 44px;
    height: 44px;
    border: 1px solid color-mix(in srgb, var(--sand) 24%, transparent);
    border-radius: 50%;
  }
  .footer__social a:hover {
    color: var(--brass-300);
    border-color: var(--brass-300);
    text-decoration: none;
  }
  .footer__social :global(svg) {
    width: 20px;
    height: 20px;
    max-width: 20px;
  }
  .footer__meta {
    margin-top: 1.6rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .footer__meta dt {
    font-size: 0.66rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--brass-300);
    margin-bottom: 0.25rem;
  }
  [lang='ar'] .footer__meta dt {
    letter-spacing: 0.02em;
  }
  .footer__meta dd {
    margin: 0;
    font-size: var(--fs-small);
    color: var(--on-dark-muted);
  }

  .footer__allergen {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid color-mix(in srgb, var(--sand) 14%, transparent);
    font-size: 0.76rem;
    color: var(--on-dark-muted);
    max-width: none;
    margin-inline: auto;
    text-align: center;
    white-space: nowrap;
  }

  .footer__tagline {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin-block: clamp(2.5rem, 5vw, 4rem);
    text-align: center;
    color: var(--brass-300);
  }
  .footer__rule {
    width: min(320px, 70vw);
    justify-content: center;
  }
  .footer__rule :global(.motif) {
    color: var(--brass-300);
    flex-shrink: 0;
  }
  .footer__taglineText {
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 1.1rem + 2vw, 2.6rem);
    font-weight: 700;
    letter-spacing: 0.06em;
    color: var(--sand);
  }
  [lang='ar'] .footer__taglineText {
    font-family: var(--font-arabic-display);
  }

  .footer__bottom {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding-top: 1.5rem;
    border-top: 1px solid color-mix(in srgb, var(--sand) 14%, transparent);
  }
  .footer__copy {
    font-size: 0.76rem;
    color: var(--on-dark-muted);
  }
  .footer__legal {
    display: flex;
    flex-wrap: wrap;
    gap: 1.2rem;
    list-style: none;
    margin: 0;
    padding: 0;
    font-size: 0.76rem;
  }
  .footer__legal a {
    color: var(--on-dark-muted);
  }
  .footer__legal a:hover {
    color: var(--sand);
  }

  @media (min-width: 640px) {
    .footer__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  @media (min-width: 960px) {
    .footer__grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }
</style>
